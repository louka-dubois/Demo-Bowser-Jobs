# School Project
### Cmds for installation:
```
composer install
npm install copy .env.example .env
php artisan key:generate
New-Item -ItemType File -Force database\database.sqlite
php artisan migrate:fresh --seed
php artisan route:list
```
```
npm run dev
```
